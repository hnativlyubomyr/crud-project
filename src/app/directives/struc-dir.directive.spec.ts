import { StrucDirDirective } from './struc-dir.directive';

describe('StrucDirDirective', () => {
  it('should create an instance', () => {
    const directive = new StrucDirDirective();
    expect(directive).toBeTruthy();
  });
});
